import { Vector } from "../math/vector";

/**
 * @category Shape
 */
class Rectangle {
    size: Vector;

    constructor(size: Vector) {
        this.size = size;
    }

    render() {
        
    }
}